import 'design_system_config.dart';

class StudentDesignSystem {
  static late StudentDSConfig config;

  static void init(StudentDSConfig designSystemConfig) {
    config = designSystemConfig;
  }
}
